# Seed Asset: Load a local CSV file as a lookup table
name: ingestion.payment_lookup

type: duckdb.seed

parameters:
  path: payment_lookup.csv

# TODO: Define columns and add quality checks.
# - Lookups should have a stable primary key.
columns:
  - name: payment_type_id
    type: integer
    description: id of different payment types
    primary_key: true
    checks:
      - name: not_null
      - name: unique
  - name: payment_type_name
    type: string
    description: name of the payment type
    checks:
      - name: not_null
